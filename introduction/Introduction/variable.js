// Declaring different variables of different data types
let firstName = 'Ben' // first name of a person
let lastName = 'Simmun' // last name of a person
let country = 'United States' // country
let city = 'New York' // capital city
let years = 60 // age in years
let single = true //

console.log(firstName, lastName, country, city, years, single);

// Declaring variables with number values

let age = 100 // age in years
const gravity = 9.81 // earth gravity  in m/s2
const boilingPoint = 100 // water boiling point, temperature in °C
const PI = 3.14 // geometrical constant
console.log(gravity, boilingPoint, PI)

// Variables can also be declaring in one line separated by comma, however I recommend to use a seperate line to make code more readble
let name = 'Ben', job = 'player', live = 'New York'
console.log(name, job, live);